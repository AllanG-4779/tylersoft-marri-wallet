<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Pull Spring property into Logback context before any appender uses it -->
    <springProperty scope="context" name="appName"
                    source="spring.application.name" defaultValue="app"/>

    <conversionRule conversionWord="clr"
                    converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>

    <!-- Scans every log line and masks sensitive key-value pairs and card numbers -->
    <conversionRule conversionWord="safemsg"
                    converterClass="net.tylersoft.common.logging.SensitiveDataConverter"/>

    <property name="LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${appName}){blue} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %safemsg%wex%n"/>

    <!-- ================================================================
         Default / dev profile — coloured plain-text to stdout
    ================================================================ -->
    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${LOG_PATTERN}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ================================================================
         prod profile — structured JSON to stdout (parsed by log aggregators)
    ================================================================ -->
    <springProfile name="prod">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeCallerData>false</includeCallerData>
                <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                    <maxDepthPerCause>10</maxDepthPerCause>
                    <rootCauseFirst>true</rootCauseFirst>
                </throwableConverter>
                <!-- Replace the standard message provider with the sensitive-data converter -->
                <providers>
                    <timestamp>
                        <fieldName>timestamp</fieldName>
                    </timestamp>
                    <logLevel/>
                    <loggerName/>
                    <threadName/>
                    <message>
                        <fieldName>message</fieldName>
                        <messageConverter class="net.tylersoft.common.logging.SensitiveDataConverter"/>
                    </message>
                    <stackTrace>
                        <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                            <maxDepthPerCause>10</maxDepthPerCause>
                            <rootCauseFirst>true</rootCauseFirst>
                        </throwableConverter>
                    </stackTrace>
                    <mdc/>
                    <arguments/>
                    <tags/>
                </providers>
                <customFields>{"service":"${appName}"}</customFields>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

</configuration>
